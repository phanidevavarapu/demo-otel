apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "demo-otel.fullname" . }}-test-connection"
  labels:
    {{- include "demo-otel.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "demo-otel.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
